# ---------------------------------------------------------------------------
# requirements.txt
# Full dependency list for the VoiceBot STT-TTS + Pipeline microservice.
# Install with:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

# ── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
# Required for UploadFile + Form (multipart) support in FastAPI
python-multipart>=0.0.9

# ── Data validation ──────────────────────────────────────────────────────────
pydantic>=2.0.0

# ── TTS engine ───────────────────────────────────────────────────────────────
kokoro>=0.9.4
# Kokoro is a lightweight English TTS engine with CUDA support when available.
# On Windows, install espeak-ng if you want phoneme fallback support.

# ── STT engine (local Whisper — no API key needed) ───────────────────────────
faster-whisper>=1.0.0
# Whisper dependencies (usually auto-installed; listed for clarity)
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
noisereduce>=3.0.0
ffmpeg-python>=0.2.0   # Python bindings; also install ffmpeg binary on your OS

# ── LLM — Anthropic Claude ───────────────────────────────────────────────────
anthropic>=0.25.0

# ── Database — async MySQL via SQLAlchemy ────────────────────────────────────
sqlalchemy>=2.0.0
aiomysql>=0.2.0
# For password characters that need URL-escaping in the connection string
PyMySQL>=1.1.0         # aiomysql uses PyMySQL under the hood

# ── Environment variable loading ─────────────────────────────────────────────
python-dotenv>=1.0.0

# ── HTTP client for ClinicQueue ──────────────────────────────────────────────
httpx>=0.27.0

# ── Transcript language filtering ─────────────────────────────────────────────
langdetect>=1.0.9

# ------------------
# Production STT helpers
# ------------------
webrtcvad>=2.0.10
soundfile>=0.12.1
noisereduce>=2.0.1
librosa>=0.10.0
torchaudio>=2.0.2
